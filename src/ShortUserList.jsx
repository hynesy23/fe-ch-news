import React from "react";

export default function ShortUserList({ users }) {
  return (
    // <ul>
    //   {users.map(user => {
    //     return <li>{user.username}</li>;
    //   })}
    // </ul>
    <h1>USER LIST</h1>
  );
}
